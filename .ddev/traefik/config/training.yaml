#ddev-generated
# If you remove the ddev-generated line above you
# are responsible for maintaining this file. DDEV will not then
# update it, for example if you add `additional_hostnames`, etc.

http:
  routers:
    training-web-80-http:
      entrypoints:
        - http-80
      rule: HostRegexp(`^training\.ddev\.site$`)
      
      service: "training-web-80"
      ruleSyntax: v3
      tls: false
      # middlewares:
      #   - "training-redirectHttps"
    training-web-8025-http:
      entrypoints:
        - http-8025
      rule: HostRegexp(`^training\.ddev\.site$`)
      
      service: "training-web-8025"
      ruleSyntax: v3
      tls: false
      # middlewares:
      #   - "training-redirectHttps"
    training-xhgui-80-http:
      entrypoints:
        - http-8143
      rule: HostRegexp(`^training\.ddev\.site$`)
      
      service: "training-xhgui-80"
      ruleSyntax: v3
      tls: false
      # middlewares:
      #   - "training-redirectHttps"
    
    
    
    training-web-80-https:
      entrypoints:
        - http-443
      rule: HostRegexp(`^training\.ddev\.site$`)
      
      service: "training-web-80"
      ruleSyntax: v3
      
      tls: true
      
    training-web-8025-https:
      entrypoints:
        - http-8026
      rule: HostRegexp(`^training\.ddev\.site$`)
      
      service: "training-web-8025"
      ruleSyntax: v3
      
      tls: true
      
    
    training-xhgui-80-https:
      entrypoints:
        - http-8142
      rule: HostRegexp(`^training\.ddev\.site$`)
      
      service: "training-xhgui-80"
      ruleSyntax: v3
      
      tls: true
      
    

  middlewares:
    training-redirectHttps:
      redirectScheme:
        scheme: https
        permanent: true

  services:
    training-web-80:
      loadbalancer:
        servers:
          - url: http://ddev-training-web:80
        
    training-web-8025:
      loadbalancer:
        servers:
          - url: http://ddev-training-web:8025
        
    
    
    training-xhgui-80:
      loadbalancer:
        servers:
          - url: http://ddev-training-xhgui:80
        
    
    

tls:
  certificates:
    - certFile: /mnt/ddev-global-cache/traefik/certs/training.crt
      keyFile: /mnt/ddev-global-cache/traefik/certs/training.key